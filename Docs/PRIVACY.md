# 隐私说明

- 文件名、元数据、PDF 文字、OCR 结果和目录画像只在本机处理。
- V2.0 没有网络 AI、遥测、广告、账号或云同步代码。
- 文本提取限制为 1 MiB 输入、4,000 字符上下文；PDF 只读取首页；OCR 只用于确定性分类仍无法判断的图片。
- 数据库存放于应用支持目录 `AIFileOrganizerV2/organizer.sqlite`。
- 决策记录只用于未来离线评测，V2.0 不自动生成规则。
- 诊断信息不应包含正文；后续实现诊断导出时默认对完整文件名和路径脱敏。

